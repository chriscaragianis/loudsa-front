import React from 'react';

const SwagMessage = (props) => {
  return (
    <div className="swag-shop-message">
      Red needs green, y'all. By popular demand we are making our Louisville DSA
      swag available <span className="internet">on the actual internet</span>. All
      we need is a mailing address and your kind donation.
    </div>
  )
};

export default SwagMessage;
